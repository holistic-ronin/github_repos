package com.atipera;

import io.quarkus.test.junit.QuarkusIntegrationTest;

@QuarkusIntegrationTest
public class GitHubResourceIT extends GitHubResourceTest {
  // Execute the same tests in the packaged (or native) mode.
}
